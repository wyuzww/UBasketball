<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/textSelectColor</item>
        <item name="colorPrimaryDark">@color/textSelectColor</item>
        <item name="colorAccent">@color/textSelectColor</item>

        <!-- ListView下拉渲染颜色 -->
        <item name="android:colorEdgeEffect">@color/textSelectColor</item>
    </style>

    <style name="My_CheckBox" parent="android:style/Widget.Material.CompoundButton.CheckBox">
        <item name="android:colorControlActivated">@color/grayDark</item>
    </style>

</resources>
